# Minimal requirements derived from notebooks in this project
# Versions use conservative lower bounds to improve reproducibility while staying compatible.
# Adjust pins to exact versions after you test in a virtualenv (e.g. pip freeze).

pandas>=1.5.0,<2.0
numpy>=1.23.0,<2.0
matplotlib>=3.5.0,<4.0
seaborn>=0.12.0,<1.0
scikit-learn>=1.1.0,<2.0
scipy>=1.9.0,<2.0
joblib>=1.2.0
# Jupyter / interactive
ipython>=8.0.0
notebook>=6.5.0
jupyter>=1.0.0

# Optional / utility
# (pickle is stdlib; os/re/collections/etc. are stdlib too)

# If you later use packages like statsmodels, xgboost, lightgbm, or mlflow add them here.
